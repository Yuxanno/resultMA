# 🎯 ЗАДАНИЕ ДЛЯ AI: Комплексная оптимизация проекта

## 📊 СТАТУС: 🔄 В ПРОЦЕССЕ

**Дата создания:** 2026-02-08
**Приоритет:** 🟡 СРЕДНИЙ

---

## 🎯 ЦЕЛЬ

Провести комплексный аудит и оптимизацию проекта ResultMA для:
- Улучшения производительности
- Уменьшения количества ошибок
- Оптимизации запросов к БД
- Улучшения UX (скорость загрузки)

---

## 📋 ПЛАН ОПТИМИЗАЦИИ

### ✅ ЭТАП 1: Исправление критических проблем (ВЫПОЛНЕНО)
- [x] Отключено избыточное кэширование Redis
- [x] Исправлена загрузка блок-тестов (минимальные данные vs полные)
- [x] Добавлены логи для отладки
- [x] Отключены логи Mongoose

### 🔄 ЭТАП 2: Оптимизация серверных запросов (В ПРОЦЕССЕ)

#### 2.1 Оптимизация роутов блок-тестов
**Проблема:** Дублирование запросов, избыточная загрузка данных

**Файл:** `server/src/routes/blockTest.routes.ts`

**Что сделать:**
- [ ] Добавить query параметр `?fields=minimal|full` для GET /block-tests
- [ ] Оптимизировать populate (загружать только нужные поля)
- [ ] Добавить пагинацию для больших списков
- [ ] Кэшировать часто запрашиваемые данные (с правильной инвалидацией)

#### 2.2 Оптимизация роутов тестов
**Файл:** `server/src/routes/test.routes.ts`

**Что сделать:**
- [ ] Аналогично блок-тестам
- [ ] Оптимизировать импорт (batch операции)
- [ ] Добавить индексы в MongoDB

#### 2.3 Оптимизация загрузки студентов
**Файл:** `server/src/routes/student.routes.ts`

**Что сделать:**
- [ ] Добавить пагинацию
- [ ] Оптимизировать populate
- [ ] Кэшировать списки студентов по классам

### 🔄 ЭТАП 3: Оптимизация клиентской части

#### 3.1 Убрать дублирование запросов
**Проблема:** Страницы делают одинаковые запросы несколько раз

**Файлы:**
- `client/src/pages/teacher/BlockTestsPage.tsx`
- `client/src/pages/teacher/EditBlockTestPage.tsx`
- `client/src/pages/teacher/ConfigureBlockTestPage.tsx`

**Что сделать:**
- [ ] Использовать React Query для кэширования
- [ ] Убрать дублирующиеся запросы
- [ ] Оптимизировать prefetch

#### 3.2 Оптимизация рендеринга
**Что сделать:**
- [ ] Добавить React.memo для тяжелых компонентов
- [ ] Использовать useMemo/useCallback где нужно
- [ ] Виртуализация длинных списков (react-window)

#### 3.3 Оптимизация загрузки
**Что сделать:**
- [ ] Code splitting (lazy loading страниц)
- [ ] Оптимизация bundle size
- [ ] Сжатие изображений

### 🔄 ЭТАП 4: Оптимизация базы данных

#### 4.1 Индексы
**Что сделать:**
- [ ] Проверить существующие индексы
- [ ] Добавить составные индексы для частых запросов
- [ ] Удалить неиспользуемые индексы

#### 4.2 Запросы
**Что сделать:**
- [ ] Использовать .lean() везде где возможно
- [ ] Оптимизировать populate (только нужные поля)
- [ ] Batch операции вместо циклов

### 🔄 ЭТАП 5: Улучшение UX

#### 5.1 Loading states
**Что сделать:**
- [ ] Skeleton loaders вместо спиннеров
- [ ] Optimistic updates
- [ ] Показывать прогресс для долгих операций

#### 5.2 Error handling
**Что сделать:**
- [ ] Единая система обработки ошибок
- [ ] Понятные сообщения об ошибках
- [ ] Retry механизм для failed requests

---

## 🔧 ПРИОРИТЕТНЫЕ ИСПРАВЛЕНИЯ

### 1. Оптимизация GET /block-tests (ВЫСОКИЙ)

**Текущая проблема:**
```typescript
// Всегда возвращает минимальные данные
.select('classNumber date periodMonth periodYear createdAt _id')
```

**Решение:**
```typescript
router.get('/', authenticate, async (req: AuthRequest, res) => {
  const fields = req.query.fields as string;
  
  let query = BlockTest.find({ branchId: req.user?.branchId });
  
  if (fields === 'minimal') {
    query = query.select('classNumber date periodMonth periodYear createdAt _id');
  } else if (fields === 'full') {
    query = query.populate('subjectTests.subjectId', 'nameUzb nameRu');
  }
  
  const blockTests = await query.sort({ date: -1 }).lean().exec();
  
  res.setHeader('Cache-Control', 'no-cache, no-store, must-revalidate');
  res.json(blockTests);
});
```

### 2. Убрать дублирование в BlockTestsPage (ВЫСОКИЙ)

**Текущая проблема:**
- Загружается список блок-тестов (минимальные данные)
- Потом загружается полный блок-тест
- Потом снова загружается список для объединения

**Решение:**
- Использовать только GET /block-tests/:id
- Убрать лишние запросы

### 3. Добавить React Query (СРЕДНИЙ)

**Преимущества:**
- Автоматическое кэширование
- Автоматическая инвалидация
- Retry механизм
- Loading/Error states из коробки

---

## 📊 МЕТРИКИ ДО/ПОСЛЕ

### Производительность
- [ ] Время загрузки списка блок-тестов: ??? → ???
- [ ] Время открытия модального окна: ??? → ???
- [ ] Количество запросов на страницу: ??? → ???

### Размер bundle
- [ ] Client bundle size: ??? → ???
- [ ] Количество chunks: ??? → ???

### База данных
- [ ] Среднее время запроса: ??? → ???
- [ ] Количество индексов: ??? → ???

---

## ✅ КРИТЕРИИ УСПЕХА

- [ ] Нет дублирующихся запросов
- [ ] Все списки загружаются < 500ms
- [ ] Модальные окна открываются мгновенно
- [ ] Bundle size < 500KB (gzipped)
- [ ] Нет ошибок в консоли
- [ ] Все запросы используют индексы

---

## 📝 ЗАМЕТКИ

- Не ломать существующий функционал
- Тестировать каждое изменение
- Добавлять логи для отладки
- Документировать изменения

---

**Последнее обновление:** 2026-02-08
