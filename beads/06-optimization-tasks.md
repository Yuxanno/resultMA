# 🎯 ЗАДАЧИ ПО ОПТИМИЗАЦИИ ПРОЕКТА

## 📊 ОБЩИЙ СТАТУС: ✅ ОСНОВНЫЕ ОПТИМИЗАЦИИ ВЫПОЛНЕНЫ

**Дата создания:** 2026-02-08
**Дата обновления:** 2026-02-08

---

## 🏗️ ЗАДАЧА 1: Оптимизация структуры кода

### 📋 Подзадачи:

#### 1.1 Создать shared типы (ВЫСОКИЙ)
**Проблема:** Дублирование типов между клиентом и сервером

**Что сделать:**
- [ ] Создать `shared/types/` папку в корне проекта
- [ ] Вынести общие типы (User, Test, BlockTest, Student и т.д.)
- [ ] Настроить TypeScript path aliases
- [ ] Обновить импорты в клиенте и сервере

**Файлы:**
```
shared/
  types/
    user.types.ts
    test.types.ts
    blockTest.types.ts
    student.types.ts
    common.types.ts
```

**Польза:**
- Единый источник правды для типов
- Меньше ошибок при изменении API
- Автокомплит работает лучше

---

#### 1.2 Создать API клиент с типизацией (ВЫСОКИЙ)
**Проблема:** `api.get()`, `api.post()` без типов

**Что сделать:**
- [ ] Создать типизированный API клиент
- [ ] Добавить типы для всех endpoints
- [ ] Добавить автоматическую обработку ошибок

**Пример:**
```typescript
// client/src/lib/apiClient.ts
import { BlockTest, Test, Student } from '@shared/types';

export const apiClient = {
  blockTests: {
    getAll: (fields?: 'minimal' | 'full') => 
      api.get<BlockTest[]>('/block-tests', { params: { fields } }),
    getById: (id: string) => 
      api.get<BlockTest>(`/block-tests/${id}`),
    create: (data: CreateBlockTestDto) => 
      api.post<BlockTest>('/block-tests', data),
  },
  tests: {
    // аналогично
  }
};
```

**Польза:**
- Типобезопасность
- Автокомплит для API
- Меньше ошибок

---

#### 1.3 Рефакторинг больших компонентов (СРЕДНИЙ)
**Проблема:** Компоненты > 500 строк

**Файлы для рефакторинга:**
- `client/src/pages/teacher/BlockTestsPage.tsx` (700+ строк)
- `client/src/pages/teacher/ConfigureBlockTestPage.tsx` (800+ строк)
- `client/src/components/GroupConfigModal.tsx` (500+ строк)

**Что сделать:**
- [ ] Разбить на подкомпоненты
- [ ] Вынести логику в custom hooks
- [ ] Создать отдельные файлы для типов

**Пример структуры:**
```
pages/teacher/BlockTestsPage/
  index.tsx              (главный компонент, 100 строк)
  BlockTestList.tsx      (список)
  BlockTestCard.tsx      (карточка)
  BlockTestFilters.tsx   (фильтры)
  useBlockTests.ts       (логика)
  types.ts               (типы)
```

**Польза:**
- Легче читать код
- Легче тестировать
- Переиспользование компонентов

---

## 🖥️ ЗАДАЧА 2: Оптимизация серверной части

### 📋 Подзадачи:

#### 2.1 Убрать node-cache, использовать Redis (ВЫСОКИЙ)
**Проблема:** Два кэша (node-cache + Redis), сложная инвалидация

**Что сделать:**
- [ ] Удалить `node-cache` зависимость
- [ ] Удалить `server/src/utils/cache.ts`
- [ ] Использовать только Redis для кэширования
- [ ] Настроить TTL и инвалидацию

**Файлы для изменения:**
- `server/src/middleware/cache.ts` - переписать на Redis
- Все роуты где используется `cacheMiddleware`

**Польза:**
- Один источник кэша
- Проще инвалидация
- Меньше багов

---

#### 2.2 Добавить индексы в MongoDB (ВЫСОКИЙ) ✅ ВЫПОЛНЕНО
**Проблема:** Медленные запросы без индексов

**Что сделано:**
- [x] Создан скрипт `server/src/scripts/createOptimizedIndexes.ts`
- [x] Добавлена команда `npm run optimize-indexes`
- [x] Исправлено подключение к БД (добавлен правильный путь к .env)
- [x] Улучшена обработка ошибок и логирование
- [x] Созданы составные индексы для всех коллекций:
  - Tests: branchId + createdBy + createdAt
  - BlockTests: branchId + classNumber + date
  - Students: branchId + classNumber
  - StudentVariants: testId + studentId
  - StudentTestConfigs: studentId + blockTestId
  - Groups: branchId + classNumber

**Как запустить:**
```bash
cd server
npm run optimize-indexes
```

**ВАЖНО:** Перед запуском проверьте `server/.env` - там должен быть правильный `MONGODB_URI`

**Польза:**
- Запросы в 10-100 раз быстрее
- Меньше нагрузка на CPU
- Меньше использование памяти

---

#### 2.3 Оптимизация импорта тестов (СРЕДНИЙ)
**Проблема:** Импорт по одному тесту, медленно для больших файлов

**Что сделать:**
- [ ] Batch операции для вставки вопросов
- [ ] Использовать `insertMany` вместо циклов
- [ ] Добавить прогресс-бар на клиенте

**Файлы:**
- `server/src/routes/test.routes.ts` (POST /import/confirm)
- `server/src/routes/blockTest.routes.ts` (POST /import/confirm)

**Польза:**
- Импорт в 5-10 раз быстрее
- Лучший UX

---

#### 2.4 Добавить пагинацию (СРЕДНИЙ)
**Проблема:** Загружаются все записи сразу

**Что сделать:**
- [ ] Добавить query параметры `?page=1&limit=20`
- [ ] Вернуть метаданные (total, pages)
- [ ] Обновить клиент для работы с пагинацией

**Endpoints для пагинации:**
- GET /students
- GET /tests
- GET /block-tests
- GET /assignments

**Польза:**
- Быстрая загрузка
- Меньше трафика

---

#### 2.5 Оптимизация populate (ВЫСОКИЙ)
**Проблема:** Загружаются все поля связанных документов

**Что сделать:**
- [ ] Указывать только нужные поля в populate
- [ ] Использовать виртуальные поля где возможно

**Пример:**
```typescript
// Плохо
.populate('groupId')
.populate('subjectId')

// Хорошо
.populate('groupId', 'name classNumber letter')
.populate('subjectId', 'nameUzb nameRu')
```

**Файлы для проверки:**
- Все роуты с `.populate()`

**Польза:**
- Меньше данных передается
- Быстрее запросы

---

## 💻 ЗАДАЧА 3: Оптимизация клиентской части

### 📋 Подзадачи:

#### 3.1 Добавить React Query (ВЫСОКИЙ) ✅ ВЫПОЛНЕНО
**Проблема:** Ручное управление кэшем, дублирующиеся запросы

**Что сделано:**
- [x] Установлен `@tanstack/react-query`
- [x] Настроен QueryClient в `client/src/lib/queryClient.ts`
- [x] Созданы custom hooks для API:
  - `client/src/hooks/useBlockTests.ts` - хуки для блок-тестов
  - `client/src/hooks/useTests.ts` - хуки для тестов
  - `client/src/hooks/useStudents.ts` - хуки для студентов
- [x] Обновлены компоненты для использования React Query:
  - `BlockTestsPage.tsx` - использует `useBlockTests()` и `useDeleteBlockTest()`
  - `TestsPage.tsx` - использует `useTests()` и `useDeleteTest()`
  - `ImportBlockTestPage.tsx` - использует `useImportBlockTest()` и `useGenerateVariants()`
  - `ImportTestPage.tsx` - использует `useImportTest()`

**Пример:**
```typescript
// hooks/useBlockTests.ts
export const useBlockTests = () => {
  return useQuery({
    queryKey: ['blockTests'],
    queryFn: () => apiClient.blockTests.getAll('minimal'),
    staleTime: 30000, // 30 секунд
  });
};

// В компоненте
const { data: blockTests, isLoading, error } = useBlockTests();
```

**Польза:**
- Автоматическое кэширование
- Автоматическая инвалидация
- Меньше кода
- Лучший UX

---

#### 3.2 Code splitting и lazy loading (СРЕДНИЙ) ✅ ВЫПОЛНЕНО
**Проблема:** Большой bundle, долгая загрузка

**Что сделано:**
- [x] Lazy load всех страниц в App.tsx
- [x] Lazy load layouts (SuperAdminLayout, CustomRoleLayout, TeacherLayout)
- [x] Suspense с Loading компонентом

**Результат:**
- Быстрая начальная загрузка
- Меньше bundle size
- Страницы загружаются по требованию

---

#### 3.3 Виртуализация списков (СРЕДНИЙ)
**Проблема:** Медленный рендеринг больших списков (100+ студентов)

**Что сделать:**
- [ ] Установить `react-window` или `react-virtual`
- [ ] Виртуализировать списки студентов
- [ ] Виртуализировать списки тестов

**Файлы:**
- `client/src/pages/teacher/ConfigureBlockTestPage.tsx` (список студентов)
- `client/src/pages/branch/StudentsPage.tsx`

**Польза:**
- Рендеринг 1000+ элементов без лагов
- Меньше использование памяти

---

#### 3.4 Оптимизация ре-рендеров (СРЕДНИЙ)
**Проблема:** Лишние ре-рендеры компонентов

**Что сделать:**
- [ ] Добавить React.memo для тяжелых компонентов
- [ ] Использовать useMemo для вычислений
- [ ] Использовать useCallback для функций

**Пример:**
```typescript
// Тяжелый компонент
const StudentCard = React.memo(({ student }) => {
  return <div>...</div>;
});

// Вычисления
const filteredStudents = useMemo(() => {
  return students.filter(s => s.name.includes(search));
}, [students, search]);

// Функции
const handleClick = useCallback(() => {
  // ...
}, [dependency]);
```

**Польза:**
- Быстрее рендеринг
- Плавнее UI

---

#### 3.5 Убрать избыточные логи (НИЗКИЙ)
**Проблема:** Консоль засорена логами

**Что сделать:**
- [ ] Убрать `console.log` из production
- [ ] Оставить только важные логи (ошибки)
- [ ] Использовать debug библиотеку

**Пример:**
```typescript
// utils/logger.ts
export const logger = {
  debug: (...args: any[]) => {
    if (import.meta.env.DEV) {
      console.log(...args);
    }
  },
  error: (...args: any[]) => {
    console.error(...args);
  }
};
```

**Польза:**
- Чистая консоль
- Легче отлаживать

---

## 🎨 ЗАДАЧА 4: Улучшение UX

### 📋 Подзадачи:

#### 4.1 Skeleton loaders (СРЕДНИЙ)
**Проблема:** Пустые экраны во время загрузки

**Что сделать:**
- [ ] Создать Skeleton компоненты
- [ ] Заменить спиннеры на скелетоны
- [ ] Добавить анимацию

**Компоненты:**
- `SkeletonCard` - для карточек
- `SkeletonTable` - для таблиц
- `SkeletonList` - для списков

**Польза:**
- Лучший UX
- Кажется быстрее

---

#### 4.2 Optimistic updates (СРЕДНИЙ)
**Проблема:** Долгое ожидание после действий

**Что сделать:**
- [ ] Мгновенное обновление UI
- [ ] Откат при ошибке
- [ ] Использовать React Query mutations

**Пример:**
```typescript
const mutation = useMutation({
  mutationFn: apiClient.blockTests.create,
  onMutate: async (newBlockTest) => {
    // Оптимистичное обновление
    queryClient.setQueryData(['blockTests'], (old) => [...old, newBlockTest]);
  },
  onError: (err, newBlockTest, context) => {
    // Откат при ошибке
    queryClient.setQueryData(['blockTests'], context.previousBlockTests);
  }
});
```

**Польза:**
- Мгновенный отклик
- Лучший UX

---

#### 4.3 Прогресс-бары для долгих операций (НИЗКИЙ)
**Проблема:** Непонятно сколько ждать

**Что сделать:**
- [ ] Добавить прогресс для импорта
- [ ] Добавить прогресс для генерации вариантов
- [ ] Показывать оставшееся время

**Польза:**
- Понятно сколько ждать
- Меньше беспокойства

---

## 🔒 ЗАДАЧА 5: Улучшение надежности

### 📋 Подзадачи:

#### 5.1 Единая обработка ошибок (ВЫСОКИЙ)
**Проблема:** Разные способы обработки ошибок

**Что сделать:**
- [ ] Создать ErrorBoundary компонент
- [ ] Единый формат ошибок с сервера
- [ ] Понятные сообщения пользователю

**Пример:**
```typescript
// server/src/middleware/errorHandler.ts
export const errorHandler = (err, req, res, next) => {
  const error = {
    message: err.message,
    code: err.code || 'INTERNAL_ERROR',
    status: err.status || 500,
    ...(process.env.NODE_ENV === 'development' && { stack: err.stack })
  };
  
  res.status(error.status).json(error);
};
```

**Польза:**
- Легче отлаживать
- Лучший UX

---

#### 5.2 Retry механизм (СРЕДНИЙ)
**Проблема:** Запросы падают при плохом интернете

**Что сделать:**
- [ ] Автоматический retry для failed requests
- [ ] Exponential backoff
- [ ] Показывать статус retry

**React Query делает это автоматически:**
```typescript
useQuery({
  queryKey: ['blockTests'],
  queryFn: fetchBlockTests,
  retry: 3,
  retryDelay: (attemptIndex) => Math.min(1000 * 2 ** attemptIndex, 30000),
});
```

**Польза:**
- Меньше ошибок
- Работает при плохом интернете

---

#### 5.3 Валидация данных (ВЫСОКИЙ)
**Проблема:** Нет валидации на клиенте и сервере

**Что сделать:**
- [ ] Установить `zod` для валидации
- [ ] Создать схемы валидации
- [ ] Валидировать на клиенте и сервере

**Пример:**
```typescript
// shared/validation/blockTest.schema.ts
import { z } from 'zod';

export const createBlockTestSchema = z.object({
  classNumber: z.number().min(1).max(11),
  periodMonth: z.number().min(1).max(12),
  periodYear: z.number().min(2020),
  subjectTests: z.array(z.object({
    subjectId: z.string(),
    questions: z.array(z.any()).min(1)
  }))
});
```

**Польза:**
- Меньше ошибок
- Типобезопасность
- Понятные сообщения об ошибках

---

## 📊 ЗАДАЧА 6: Мониторинг и аналитика

### 📋 Подзадачи:

#### 6.1 Логирование производительности (СРЕДНИЙ)
**Проблема:** Не знаем какие запросы медленные

**Что сделать:**
- [ ] Логировать время выполнения запросов
- [ ] Логировать медленные запросы (> 1s)
- [ ] Создать dashboard для мониторинга

**Уже есть в коде:**
```typescript
// server/src/index.ts
res.on('finish', () => {
  const duration = Date.now() - start;
  logger.apiResponse(req.method, req.path, res.statusCode, duration);
});
```

**Что добавить:**
- [ ] Сохранять в файл или БД
- [ ] Анализировать статистику

**Польза:**
- Видим узкие места
- Можем оптимизировать

---

#### 6.2 Error tracking (НИЗКИЙ)
**Проблема:** Не знаем о всех ошибках пользователей

**Что сделать:**
- [ ] Интегрировать Sentry или аналог
- [ ] Отправлять ошибки на сервер
- [ ] Создать dashboard ошибок

**Польза:**
- Знаем о всех ошибках
- Можем быстро исправить

---

## 🎯 ПРИОРИТИЗАЦИЯ ЗАДАЧ

### 🔴 Критические (делать первыми):
1. ✅ Исправить баги с загрузкой тестов (ВЫПОЛНЕНО)
2. ✅ Добавить React Query (ЗАДАЧА 3.1) - ВЫПОЛНЕНО
3. ✅ Добавить MongoDB индексы (ЗАДАЧА 2.2) - ВЫПОЛНЕНО
4. Убрать node-cache (ЗАДАЧА 2.1)
5. Единая обработка ошибок (ЗАДАЧА 5.1)

### 🟡 Важные (делать после критических):
6. Создать shared типы (ЗАДАЧА 1.1)
7. Типизированный API клиент (ЗАДАЧА 1.2)
8. Оптимизация populate (ЗАДАЧА 2.5)
9. Рефакторинг больших компонентов (ЗАДАЧА 1.3)
10. Code splitting (ЗАДАЧА 3.2)

### 🟢 Желательные (делать когда будет время):
11. Виртуализация списков (ЗАДАЧА 3.3)
12. Skeleton loaders (ЗАДАЧА 4.1)
13. Пагинация (ЗАДАЧА 2.4)
14. Оптимизация импорта (ЗАДАЧА 2.3)
15. Остальные задачи

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После выполнения всех задач:

### Производительность:
- ⚡ Загрузка страниц: < 500ms
- ⚡ API запросы: < 200ms
- ⚡ Bundle size: < 500KB (gzipped)
- ⚡ Рендеринг списков: 60 FPS

### Качество кода:
- 📝 Типобезопасность: 100%
- 📝 Дублирование кода: < 5%
- 📝 Размер компонентов: < 300 строк
- 📝 Покрытие тестами: > 70%

### UX:
- 😊 Skeleton loaders везде
- 😊 Мгновенный отклик (optimistic updates)
- 😊 Понятные ошибки
- 😊 Прогресс-бары для долгих операций

---

**Последнее обновление:** 2026-02-08
