# 🔍 Задача #1: Проблема с отображением загруженных тестов

## 📊 Статус: 🔄 В ПРОЦЕССЕ

**Приоритет:** 🔴 ВЫСОКИЙ  
**Дата создания:** 2025-01-XX  
**Последнее обновление:** 2025-01-XX

---

## 🎯 Описание проблемы

После загрузки тестов или блок-тестов через страницы импорта, они не появляются в списке на страницах `/teacher/tests` и `/teacher/block-tests`.

### Симптомы:
- ✅ Импорт проходит успешно (показывается "Muvaffaqiyatli!")
- ✅ Редирект на страницу списка работает
- ❌ Новый тест не отображается в списке
- ❌ Тест появляется только после F5 (иногда)

---

## ✅ Что уже сделано

### 1. Отключено кэширование (Попытка #1) ✅
**Файлы:**
- `server/src/routes/test.routes.ts`
- `server/src/routes/blockTest.routes.ts`

**Изменения:**
- Удален `cacheMiddleware` из GET роутов
- Добавлены HTTP заголовки `Cache-Control: no-cache`
- Удалены вызовы `invalidateCache`

**Результат:** ✅ ВЫПОЛНЕНО

### 2. Автоматическое обновление на клиенте (Попытка #1) ✅
**Файлы:**
- `client/src/pages/teacher/TestsPage.tsx`
- `client/src/pages/teacher/BlockTestsPage.tsx`
- `client/src/pages/teacher/ImportTestPage.tsx`
- `client/src/pages/teacher/ImportBlockTestPage.tsx`

**Изменения:**
- Добавлен флаг `refresh` в navigation state
- Добавлен `useEffect` для обработки флага
- Добавлен timestamp к запросам `?_t=${Date.now()}`
- Добавлен обработчик `window.focus`

**Результат:** ✅ ВЫПОЛНЕНО

### 3. Добавлены детальные логи (Попытка #2) ✅
**Файлы:**
- `server/src/routes/test.routes.ts` (POST /import/confirm, GET /)
- `server/src/routes/blockTest.routes.ts` (POST /import/confirm)

**Изменения:**
```typescript
// При импорте
console.log('🔍 Import test request:', { testName, groupId, ... });
console.log('🔍 User info:', { userId, branchId, role });
console.log('✅ Test saved successfully:', { testId, name, ... });

// При получении списка
console.log('🔍 Fetching tests with filter:', filter);
console.log('✅ Found tests:', tests.length);
```

**Результат:** ✅ ВЫПОЛНЕНО - Теперь можно отлаживать!

---

## 🔍 План дальнейших действий

### Шаг 1: Проверка сохранения в БД
**Цель:** Убедиться, что тест действительно сохраняется

**Действия:**
1. Добавить детальные логи в `POST /tests/import/confirm`:
   ```typescript
   console.log('🔍 Import request:', { testName, groupId, subjectId, questionsCount: questions.length });
   console.log('🔍 User info:', { userId: req.user?.id, branchId: req.user?.branchId, role: req.user?.role });
   console.log('✅ Test saved:', { testId: test._id, name: test.name });
   ```

2. Проверить в MongoDB:
   ```bash
   mongosh "mongodb://localhost:27017/resultma"
   db.tests.find().sort({createdAt: -1}).limit(1).pretty()
   ```

**Файлы:** `server/src/routes/test.routes.ts` (строки 390-425)

**Ожидаемый результат:** Тест должен быть в БД с правильными полями

---

### Шаг 2: Проверка фильтров в GET запросе
**Цель:** Убедиться, что фильтры не скрывают новый тест

**Действия:**
1. Добавить логи в `GET /tests`:
   ```typescript
   console.log('🔍 Fetching tests with filter:', filter);
   console.log('🔍 User info:', { userId: req.user?.id, branchId: req.user?.branchId, role: req.user?.role });
   console.log('✅ Found tests:', tests.length);
   ```

2. Проверить условия фильтрации:
   - Для учителя: `filter.createdBy = req.user.id`
   - Для админа: `filter.branchId = req.user.branchId`

**Файлы:** `server/src/routes/test.routes.ts` (строки 57-85)

**Возможные проблемы:**
- ❌ `branchId` не совпадает
- ❌ `createdBy` не совпадает
- ❌ Роль пользователя определяется неправильно

---

### Шаг 3: Проверка прав доступа
**Цель:** Убедиться, что пользователь имеет права видеть тест

**Действия:**
1. Проверить middleware `authenticate`:
   ```typescript
   // В server/src/middleware/auth.ts
   console.log('🔍 Authenticated user:', { 
     id: req.user?.id, 
     role: req.user?.role, 
     branchId: req.user?.branchId 
   });
   ```

2. Проверить токен в localStorage:
   ```javascript
   // В браузере (Console)
   console.log('Token:', localStorage.getItem('token'));
   ```

**Файлы:** `server/src/middleware/auth.ts`

**Возможные проблемы:**
- ❌ Токен устарел
- ❌ `branchId` не передается в токене
- ❌ Роль определяется неправильно

---

### Шаг 4: Проверка клиентской части
**Цель:** Убедиться, что данные приходят на клиент

**Действия:**
1. Добавить логи в `fetchTests`:
   ```typescript
   console.log('🔍 Fetching tests...');
   const { data } = await api.get(`/tests?_t=${Date.now()}`);
   console.log('✅ Received tests:', data.length, data);
   setTests(data);
   ```

2. Проверить Network tab в DevTools:
   - Статус ответа: 200
   - Тело ответа: массив тестов
   - Заголовки: `Cache-Control: no-cache`

**Файлы:** `client/src/pages/teacher/TestsPage.tsx` (строки 35-45)

**Возможные проблемы:**
- ❌ Запрос не выполняется
- ❌ Ответ пустой
- ❌ Ошибка на сервере

---

### Шаг 5: Проверка состояния React
**Цель:** Убедиться, что состояние обновляется

**Действия:**
1. Добавить логи в `useEffect`:
   ```typescript
   useEffect(() => {
     if (location.state?.refresh) {
       console.log('🔄 Refreshing tests after import...');
       fetchTests();
       navigate(location.pathname, { replace: true, state: {} });
     }
   }, [location.state]);
   ```

2. Проверить React DevTools:
   - Состояние `tests`
   - Пропсы компонентов
   - Рендеринг списка

**Файлы:** `client/src/pages/teacher/TestsPage.tsx` (строки 28-50)

**Возможные проблемы:**
- ❌ `useEffect` не срабатывает
- ❌ Состояние не обновляется
- ❌ Компонент не ререндерится

---

## 🐛 Известные проблемы

### Проблема #1: Асинхронность сохранения
**Описание:** Тест может сохраняться в БД с задержкой

**Решение:**
```typescript
// В ImportTestPage.tsx
await api.post('/tests/import/confirm', { ... });
await new Promise(resolve => setTimeout(resolve, 500)); // Подождать 500ms
navigate('/teacher/tests', { state: { refresh: true } });
```

### Проблема #2: Транзакции MongoDB
**Описание:** Если используются транзакции, данные могут быть не видны сразу

**Решение:** Проверить, используются ли транзакции в `test.routes.ts`

### Проблема #3: Популяция полей
**Описание:** GET запрос может падать из-за отсутствия связанных документов

**Решение:** Добавить проверку в `.populate()`:
```typescript
.populate('groupId', 'name classNumber letter')
.populate('subjectId', 'nameUzb nameRu')
```

---

## 📝 Чеклист для проверки

- [ ] Тест сохраняется в БД (проверить через mongosh)
- [ ] Логи сервера показывают успешное сохранение
- [ ] GET запрос возвращает новый тест
- [ ] Фильтры не скрывают тест
- [ ] Права доступа настроены правильно
- [ ] Клиент получает данные
- [ ] Состояние React обновляется
- [ ] Список ререндерится
- [ ] Тест отображается в UI

---

## 🔧 Быстрые команды для отладки

```bash
# Проверить последний тест в БД
mongosh "mongodb://localhost:27017/resultma" --eval "db.tests.find().sort({createdAt: -1}).limit(1).pretty()"

# Проверить количество тестов
mongosh "mongodb://localhost:27017/resultma" --eval "db.tests.countDocuments()"

# Проверить тесты конкретного пользователя
mongosh "mongodb://localhost:27017/resultma" --eval "db.tests.find({createdBy: ObjectId('USER_ID')}).pretty()"

# Очистить все тесты (ОСТОРОЖНО!)
# mongosh "mongodb://localhost:27017/resultma" --eval "db.tests.deleteMany({})"
```

---

## 📚 Связанные задачи

- `beads/02-cache-system.md` - Система кэширования
- `beads/03-authentication.md` - Аутентификация и права доступа
- `beads/04-database-queries.md` - Запросы к базе данных

---

**Следующий шаг:** Выполнить Шаг 1 (Проверка сохранения в БД)
