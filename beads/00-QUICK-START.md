# 🚀 ЗАДАНИЕ ДЛЯ AI: Отладка проблемы с тестами

## 🎯 ТВОЯ ЗАДАЧА
Выяснить, почему тесты не появляются в списке после загрузки, и исправить проблему.

## 📊 СТАТУС: 🔄 В ПРОЦЕССЕ

---

## ✅ ЧТО УЖЕ СДЕЛАНО (не делай это снова!)

1. ✅ Отключено кэширование Redis для тестов
2. ✅ Добавлены HTTP заголовки `Cache-Control: no-cache`
3. ✅ Добавлено автоматическое обновление на клиенте
4. ✅ Добавлены детальные логи с эмодзи

---

## 🔴 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### ШАГ 1: Попроси пользователя запустить сервер и проверить логи

**Скажи пользователю:**
```
Запусти сервер командой:
cd server
npm run dev

Затем попробуй загрузить тест и скопируй сюда логи из терминала.
```

**Жди ответа пользователя с логами.**

---

### ШАГ 2: Анализируй логи сервера

**Что искать в логах:**

#### Если видишь:
```
🔍 Import test request: { ... }
✅ Test saved successfully: { testId: '...', ... }
```
✅ **Сохранение работает** → Переходи к ШАГ 3

#### Если видишь:
```
❌ Error saving imported test: ...
```
❌ **Ошибка сохранения** → Исправь ошибку в `server/src/routes/test.routes.ts`

#### Если логов нет вообще:
❌ **Запрос не доходит до сервера** → Проверь клиентскую часть

---

### ШАГ 3: Попроси пользователя проверить MongoDB

**Скажи пользователю:**
```
Выполни команду:
mongosh "mongodb://localhost:27017/resultma" --eval "db.tests.find().sort({createdAt: -1}).limit(1).pretty()"

Скопируй результат сюда.
```

**Анализируй результат:**

#### Если тест ЕСТЬ в БД:
```javascript
{
  _id: ObjectId("..."),
  name: "Test name",
  branchId: ObjectId("..."),
  createdBy: ObjectId("..."),
  ...
}
```
✅ **Тест сохранен** → Проблема в фильтрах или правах → Переходи к ШАГ 4

#### Если теста НЕТ:
❌ **Тест не сохраняется** → Вернись к ШАГ 2, проверь ошибки

---

### ШАГ 4: Проверь фильтры в GET запросе

**Попроси пользователя:**
```
Открой страницу /teacher/tests и скопируй логи из терминала сервера.
```

**Анализируй логи:**
```
🔍 Fetching tests with filter: { branchId: '...', createdBy: '...' }
🔍 User info: { userId: '...', branchId: '...', role: 'TEACHER' }
✅ Found tests: 5
```

**Сравни:**
- `filter.branchId` == `test.branchId` из MongoDB?
- `filter.createdBy` == `test.createdBy` из MongoDB?

#### Если НЕ совпадают:
❌ **Проблема в правах доступа** → Переходи к ШАГ 5

#### Если совпадают, но `Found tests: 0`:
❌ **Проблема в запросе к БД** → Проверь `server/src/routes/test.routes.ts` строка 57-85

#### Если `Found tests: 5` но в UI не показывается:
❌ **Проблема на клиенте** → Переходи к ШАГ 6

---

### ШАГ 5: Проверь токен пользователя

**Попроси пользователя:**
```
Открой консоль браузера (F12) и выполни:
const token = localStorage.getItem('token');
const payload = JSON.parse(atob(token.split('.')[1]));
console.log(payload);

Скопируй результат сюда.
```

**Анализируй payload:**
```javascript
{
  id: "...",
  role: "TEACHER",
  branchId: "...",  // ← Должен быть!
  iat: 1234567890,
  exp: 1234567890
}
```

#### Если `branchId` отсутствует:
❌ **Токен неправильный** → Проверь `server/src/routes/auth.routes.ts` (или где создается токен)

**Исправь создание токена:**
```typescript
const token = jwt.sign(
  { 
    id: user._id, 
    role: user.role,
    branchId: user.branchId  // ← Добавь это!
  },
  JWT_SECRET,
  { expiresIn: '24h' }
);
```

**Скажи пользователю перелогиниться.**

---

### ШАГ 6: Проверь клиентскую часть

**Попроси пользователя:**
```
Открой консоль браузера (F12) и скопируй все логи сюда.
```

**Что искать:**

#### Должно быть:
```
🔄 Refreshing tests after import...
✅ Received tests: 5 [...]
```

#### Если видишь ошибку:
```
❌ Error fetching tests: ...
```
→ Исправь ошибку в `client/src/pages/teacher/TestsPage.tsx`

#### Если логов нет:
→ Проверь, срабатывает ли `useEffect` в `TestsPage.tsx`

---

### ШАГ 7: Проверь Network tab

**Попроси пользователя:**
```
Открой DevTools → Network → найди запрос GET /api/tests
Скопируй:
1. Status (должен быть 200)
2. Response (тело ответа)
```

**Анализируй:**

#### Status 200, Response: `[{...}, {...}]`
✅ **Данные приходят** → Проблема в рендеринге → Проверь `TestsPage.tsx`

#### Status 401
❌ **Не авторизован** → Вернись к ШАГ 5

#### Status 500
❌ **Ошибка сервера** → Вернись к ШАГ 2

#### Response: `[]`
❌ **Фильтры скрывают тесты** → Вернись к ШАГ 4

---

## 🔧 ВОЗМОЖНЫЕ ИСПРАВЛЕНИЯ

### Исправление #1: Фильтр по branchId неправильный
**Файл:** `server/src/routes/test.routes.ts`

```typescript
// Проверь, что фильтр применяется правильно
const filter: any = {};

if (req.user?.branchId) {
  filter.branchId = req.user.branchId;
}

// Для учителей - только свои тесты
if (req.user?.role === 'TEACHER') {
  filter.createdBy = req.user.id;
}
```

### Исправление #2: Токен не содержит branchId
**Файл:** Найди где создается JWT токен (обычно `auth.routes.ts` или `login`)

```typescript
const token = jwt.sign(
  { 
    id: user._id, 
    role: user.role,
    branchId: user.branchId  // ← Добавь!
  },
  JWT_SECRET,
  { expiresIn: '24h' }
);
```

### Исправление #3: Состояние React не обновляется
**Файл:** `client/src/pages/teacher/TestsPage.tsx`

```typescript
// Проверь, что useEffect срабатывает
useEffect(() => {
  if (location.state?.refresh) {
    console.log('🔄 Refreshing tests after import...');
    fetchTests();
    navigate(location.pathname, { replace: true, state: {} });
  }
}, [location.state]);
```

---

## 📝 ЧЕКЛИСТ ДЛЯ ТЕБЯ (AI)

- [ ] Попросил пользователя запустить сервер
- [ ] Получил логи сервера
- [ ] Проверил, сохраняется ли тест в БД
- [ ] Проверил фильтры в GET запросе
- [ ] Проверил токен пользователя
- [ ] Проверил логи браузера
- [ ] Проверил Network tab
- [ ] Нашел причину проблемы
- [ ] Исправил проблему
- [ ] Попросил пользователя проверить снова
- [ ] **ОБНОВИЛ СТАТУС на ✅ ВЫПОЛНЕНО в этом файле**
- [ ] **НЕ СОЗДАЛ лишние .md файлы**

---

## ✅ ПОСЛЕ ЗАВЕРШЕНИЯ

1. Измени статус в начале файла:
   ```markdown
   ## 📊 СТАТУС: ✅ ВЫПОЛНЕНО
   **Дата завершения:** 2025-01-XX
   ```

2. Обнови AGENTS.md - измени текущую задачу на следующую

3. Сообщи пользователю результат (БЕЗ создания .md файлов!)

4. **НЕ создавай** файлы типа:
   - ❌ `TEST_IMPORT_FIX.md`
   - ❌ `SOLUTION_SUMMARY.md`
   - ❌ `CACHE_DISABLED.md`
   - ❌ Любые другие .md файлы с отчетами

---

## 💡 СОВЕТЫ ДЛЯ ТЕБЯ

1. **Не гадай** - всегда проси логи у пользователя
2. **Иди по шагам** - не пропускай проверки
3. **Логируй всё** - добавляй `console.log` везде
4. **Проверяй дважды** - сравнивай данные из разных источников
5. **Объясняй пользователю** - что ты делаешь и зачем

---

## 🆘 ЕСЛИ ЗАСТРЯЛ

**Спроси у пользователя:**
1. Логи сервера (весь вывод)
2. Логи браузера (Console)
3. Network tab (скриншот или текст)
4. Результат из MongoDB

**Проанализируй все данные вместе и найди несоответствие.**

---

**НАЧНИ С ШАГА 1!**
