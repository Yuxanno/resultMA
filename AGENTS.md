# 🤖 AI AGENT CONFIGURATION

## 📋 ОСНОВНАЯ ИНФОРМАЦИЯ

**Проект:** ResultMA - Система управления тестами для школ  
**Язык общения:** Русский (с пользователем), Узбекский (в UI)  
**Стек:** React + TypeScript (client), Node.js + Express + MongoDB (server)  
**Структура:** Monorepo (client/ и server/ в одном репозитории)

---

## 🎯 ТЕКУЩАЯ ЗАДАЧА

**Проблема:** После загрузки тестов или блок-тестов они не появляются в списке

**Статус:** 🔄 В ПРОЦЕССЕ РЕШЕНИЯ

**Что уже сделано:**
- ✅ Отключено кэширование Redis для тестов и блок-тестов
- ✅ Добавлены HTTP заголовки `Cache-Control: no-cache`
- ✅ Добавлено автоматическое обновление списка после импорта
- ✅ Добавлен timestamp к запросам для обхода кэша браузера
- ✅ Удалены все вызовы `invalidateCache` (больше не нужны)
- ✅ Добавлены детальные логи для отладки (с эмодзи)

**Что нужно сделать СЕЙЧАС:**
1. � **ЗАПУСТИТЬ СЕРВЕР** и проверить логи при импорте теста
2. � **ОТКРЫТЬ КОНСОЛЬ БРАУЗЕРА** и проверить ошибки
3. � **ПРОВЕРИТЬ NETWORK TAB** - приходят ли данные с сервера
4. � **ПРОВЕРИТЬ MONGODB** - сохраняется ли тест в БД

**Файлы для проверки:**
- `server/src/routes/test.routes.ts` - роуты для тестов
- `server/src/routes/blockTest.routes.ts` - роуты для блок-тестов
- `client/src/pages/teacher/TestsPage.tsx` - страница списка тестов
- `client/src/pages/teacher/BlockTestsPage.tsx` - страница списка блок-тестов
- `client/src/pages/teacher/ImportTestPage.tsx` - страница импорта теста
- `client/src/pages/teacher/ImportBlockTestPage.tsx` - страница импорта блок-теста

---

## 📚 НАВИГАЦИЯ ПО ЗАДАЧАМ

Все задачи и их детали находятся в папке `beads/`:

- `beads/00-QUICK-START.md` - 🚀 НАЧНИ ОТСЮДА! Пошаговая инструкция для AI
- `beads/01-test-import-issue.md` - Детальный план решения проблемы с тестами
- `beads/02-cache-system.md` - Справка по системе кэширования
- `beads/03-authentication.md` - Справка по аутентификации и правам
- `beads/04-database-queries.md` - Справка по запросам к БД

**Как использовать (для AI):**
1. Открой `beads/00-QUICK-START.md` - там пошаговый план
2. Следуй инструкциям шаг за шагом
3. Проси пользователя выполнить команды и предоставить логи
4. Анализируй логи и находи проблему
5. Исправляй код
6. Обновляй статус в `beads/*.md`

**ВАЖНО:** В beads/ находятся задания для AI, а не для пользователя!

---

## 🛠️ ПРАВИЛА РАБОТЫ

### 0. Анализ проблемы (ОБЯЗАТЕЛЬНО!)
**ПЕРЕД ЛЮБЫМ ИСПРАВЛЕНИЕМ:**
1. **Опиши проблему** - что именно не работает
2. **Найди причину** - почему это происходит (проверь код!)
3. **Предложи решение** - как это исправить правильно
4. **Объясни что будешь делать** - какие файлы изменишь и зачем
5. **ТОЛЬКО ПОТОМ** делай изменения

**Формат объяснения:**
```
## 🔍 АНАЛИЗ ПРОБЛЕМЫ

**Что не работает:** [описание]
**Причина:** [почему это происходит]
**Решение:** [как исправить]
**План действий:** [что буду делать]
```

### 1. Язык общения
- **С пользователем:** Русский (неформальный, дружелюбный)
- **В коде/комментариях:** Английский
- **В UI:** Узбекский (латиница)
- **В логах:** Английский

### 2. Стиль кода
- TypeScript strict mode
- Функциональные компоненты (React)
- Async/await вместо промисов
- Деструктуризация параметров
- Короткие, понятные имена переменных

### 3. Отладка
- Всегда добавляй `console.log` для важных операций
- Используй эмодзи в логах: ✅ успех, ❌ ошибка, 🔄 процесс, 🔍 отладка
- Логируй входные данные и результаты

### 4. Тестирование
- Проверяй изменения через DevTools (Network, Console)
- Тестируй на разных ролях (admin, teacher)
- Проверяй edge cases (пустые данные, ошибки сети)

### 5. Документация
- Обновляй `beads/*.md` после каждого изменения
- Пиши понятные commit messages
- Документируй сложную логику в комментариях

---

## 🔧 ЧАСТЫЕ КОМАНДЫ

### Запуск проекта
```bash
# Сервер (из корня)
cd server
npm run dev

# Клиент (из корня)
cd client
npm run dev
```

### Проверка кода
```bash
# TypeScript проверка
npm run type-check

# Линтинг
npm run lint
```

### База данных
```bash
# Подключение к MongoDB
mongosh "mongodb://localhost:27017/resultma"

# Проверка коллекций
show collections
db.tests.find().limit(5)
db.blockTests.find().limit(5)
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
resultMA/
├── client/                 # React фронтенд
│   ├── src/
│   │   ├── pages/         # Страницы
│   │   │   └── teacher/   # Страницы учителя
│   │   ├── components/    # Компоненты
│   │   ├── lib/          # Утилиты
│   │   └── hooks/        # React хуки
│   └── package.json
│
├── server/                # Node.js бэкенд
│   ├── src/
│   │   ├── routes/       # API роуты
│   │   ├── models/       # MongoDB модели
│   │   ├── middleware/   # Middleware
│   │   ├── services/     # Бизнес-логика
│   │   └── config/       # Конфигурация
│   └── package.json
│
├── beads/                 # 📍 Задачи и навигация
│   ├── 01-test-import-issue.md
│   ├── 02-cache-system.md
│   └── ...
│
├── AGENTS.md             # 📖 Этот файл
└── README.md             # Основная документация
```

---

## 🚨 ВАЖНЫЕ ЗАМЕЧАНИЯ

### Кэширование
- ❌ НЕ используй `cacheMiddleware` для тестов и блок-тестов
- ✅ Используй HTTP заголовки `Cache-Control: no-cache`
- ✅ Добавляй timestamp к запросам: `?_t=${Date.now()}`

### Аутентификация
- Всегда проверяй `req.user?.branchId`
- Учителя видят только свои тесты (`createdBy: req.user.id`)
- Админы видят все тесты своего филиала

### База данных
- Используй `.lean()` для оптимизации
- Используй `.select()` для выборки только нужных полей
- Всегда проверяй индексы для часто используемых запросов

### React
- Используй `useEffect` с правильными зависимостями
- Очищай подписки и таймеры в cleanup функции
- Используй `useCallback` для функций в зависимостях

---

## 📞 КОНТАКТЫ И РЕСУРСЫ

- **MongoDB:** `mongodb://localhost:27017/resultma`
- **API:** `http://localhost:5000/api`
- **Client:** `http://localhost:5173`
- **Документация API:** См. `server/src/routes/`

---

## 🔄 WORKFLOW

1. **Прочитай текущую задачу** в `beads/00-QUICK-START.md`
2. **Изучи связанные файлы** (указаны в задаче)
3. **Сделай изменения** следуя правилам
4. **Протестируй** изменения (проси пользователя выполнить команды)
5. **Обнови статус задачи** в `beads/*.md` на ✅ ВЫПОЛНЕНО
6. **Сообщи пользователю** о результате (БЕЗ создания .md файлов!)

---

## 📝 ПРАВИЛА ДОКУМЕНТАЦИИ

### ✅ ЧТО ДЕЛАТЬ:
1. **Обновляй статус** в существующих `beads/*.md` файлах
2. **Записывай новые задания** от пользователя в `beads/` (если это новая задача)
3. **Меняй статус на ✅ ВЫПОЛНЕНО** после завершения задачи

### ❌ ЧТО НЕ ДЕЛАТЬ:
1. **НЕ создавай** новые .md файлы после завершения задачи
2. **НЕ создавай** файлы типа `SUMMARY.md`, `FIX.md`, `SOLUTION.md`
3. **НЕ создавай** документацию без запроса пользователя

### 📋 Формат новой задачи в beads/:

Если пользователь дает НОВУЮ задачу, создай файл `beads/XX-название-задачи.md`:

```markdown
# 🎯 ЗАДАНИЕ ДЛЯ AI: Название задачи

## 📊 СТАТУС: 🔄 В ПРОЦЕССЕ

**Дата создания:** 2025-01-XX

---

## 🎯 ОПИСАНИЕ ЗАДАЧИ

[Что нужно сделать]

---

## 📋 ПЛАН ДЕЙСТВИЙ

### ШАГ 1: [Название]
[Что делать]

### ШАГ 2: [Название]
[Что делать]

---

## ✅ КРИТЕРИИ ВЫПОЛНЕНИЯ

- [ ] Пункт 1
- [ ] Пункт 2

---

## 📁 ФАЙЛЫ ДЛЯ ИЗМЕНЕНИЯ

- `путь/к/файлу.ts` - описание
```

### 🔄 Обновление статуса:

Когда задача выполнена, измени в начале файла:

```markdown
## 📊 СТАТУС: ✅ ВЫПОЛНЕНО

**Дата завершения:** 2025-01-XX
```

---

## 💡 СОВЕТЫ

- Если не уверен - спроси у пользователя
- Если проблема сложная - разбей на подзадачи
- Если что-то не работает - проверь логи
- Если нужна помощь - опиши что уже пробовал
- **После завершения задачи - НЕ создавай .md файлы с отчетами!**
- **Просто обнови статус в beads/ и сообщи пользователю результат**

---

## 🚫 ЗАПРЕЩЕНО

1. ❌ Создавать файлы `SUMMARY.md`, `FIX.md`, `SOLUTION.md` после задачи
2. ❌ Создавать документацию без запроса пользователя
3. ❌ Оставлять статус задачи "В ПРОЦЕССЕ" после завершения
4. ❌ Создавать дубликаты задач в beads/

---

## ✅ ОБЯЗАТЕЛЬНО

1. ✅ Обновляй статус задачи на "ВЫПОЛНЕНО" после завершения
2. ✅ Записывай НОВЫЕ задачи от пользователя в beads/
3. ✅ Следуй формату документации из раздела "ПРАВИЛА ДОКУМЕНТАЦИИ"
4. ✅ Удаляй старые ненужные .md файлы по запросу пользователя

---

**Последнее обновление:** 2025-01-XX  
**Версия:** 1.0.0
