# Скрипт повышения класса учеников

## Описание
Этот скрипт автоматически повышает класс всех учеников каждое 1 сентября.

## Логика работы

1. **Ученики 1-10 классов**: Переходят на следующий класс (например, 5 класс → 6 класс)
2. **Ученики 11 класса**: Помечаются как выпускники (`isGraduated = true`)
3. **Выпускники**: Не отображаются в обычных списках, только в админ-панелях

## Запуск скрипта

### Вручную
```bash
cd server
npx ts-node src/scripts/promoteStudents.ts
```

### Автоматически (рекомендуется)
Настройте cron job для автоматического запуска каждое 1 сентября:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (запуск 1 сентября в 00:00)
0 0 1 9 * cd /path/to/project/server && npx ts-node src/scripts/promoteStudents.ts >> /var/log/promote-students.log 2>&1
```

## Что происходит с выпускниками?

- ✅ Видны в панели Супер Админа (Foydalanuvchilar)
- ✅ Видны в панели Фил Админа (O'quvchilar)
- ❌ Не видны учителям в списках групп
- ❌ Не видны в обычных списках учеников
- 🎨 Карточки выпускников имеют другой цвет (серый/приглушенный)

## Безопасность

- Скрипт не удаляет данные учеников
- Все результаты тестов и история сохраняются
- Можно вручную изменить статус выпускника обратно при необходимости

## Откат изменений

Если нужно откатить изменения (например, запустили по ошибке):

```bash
# Понизить класс всем ученикам 2-11 классов
db.students.updateMany(
  { classNumber: { $gte: 2, $lte: 11 }, isGraduated: false },
  { $inc: { classNumber: -1 } }
)

# Вернуть выпускников в 11 класс
db.students.updateMany(
  { isGraduated: true },
  { $set: { isGraduated: false, classNumber: 11 } }
)
```

## Примечания

- Рекомендуется делать резервную копию базы данных перед запуском
- Скрипт можно запускать несколько раз - он не повысит класс повторно
- Логи выполнения сохраняются в консоль
